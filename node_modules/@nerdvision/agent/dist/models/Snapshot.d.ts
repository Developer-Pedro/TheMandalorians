import { BreakpointResponse } from '@nerdvision/grpc-api';
import Scope from './Scope';
export default class Snapshot {
    private readonly _ts;
    private readonly _stacktrace;
    private _breakpoint;
    private _named_watches;
    constructor(timestamp: number, frames: any[]);
    breakpoint: BreakpointResponse.Breakpoint;
    named_watches: Array<{}>;
    toJSON(): {
        ts: number;
        stacktrace: Scope[];
        breakpoint: any;
        named_watches: {}[];
    };
    /**
     * This function formats the breakpoint the same way the Java/Python agents do
     * It takes in an object (but typing this caused errors when doing breakpoint.args)
     *
     * @param breakpoint
     */
    private formatBreakpoint;
}
