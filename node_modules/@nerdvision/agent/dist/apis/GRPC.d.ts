import Breakpoints from '../Breakpoints';
export declare class GRPC {
    private _breakpoints;
    private client;
    private stream;
    constructor(_breakpoints: Breakpoints);
    connect(shortTimeout?: boolean): Promise<void>;
    close(): void;
}
