import Request from '../utils/Request';
import FileLoader from '../utils/FileLoader';
export default class AgentRegistration {
    private request;
    private _packagePath;
    private _fileLoader;
    private _packageJson;
    constructor(request: Request, _packagePath: string, _fileLoader: FileLoader);
    readonly packageVersion: any;
    private readonly product;
    private readonly network;
    private readonly networkInterfaces;
    private readonly lang;
    private readonly os;
    private readonly instance;
    loadPackage(): Promise<void>;
    register(shortTimeout?: boolean): Promise<string>;
    private generateAuthToken;
    private generateClientId;
}
