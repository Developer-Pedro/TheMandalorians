import ConfigureStage from './ConfigureStage';
export default class Startup {
    private _configureStage;
    private stages;
    constructor(_configureStage: ConfigureStage);
    run(): Promise<void>;
    private asyncForEach;
}
