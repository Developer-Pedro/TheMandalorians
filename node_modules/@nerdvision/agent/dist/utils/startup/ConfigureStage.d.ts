import { StartupStage } from './StartupStage';
import { NerdVisionApiKey, NerdVisionConfig } from '../../models/NerdVisionConfig';
export default class ConfigureStage extends StartupStage {
    run(config: NerdVisionApiKey | NerdVisionConfig): Promise<void>;
}
