"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : new P(function (resolve) { resolve(result.value); }).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const StartupStage_1 = require("./StartupStage");
const Configuration_1 = __importDefault(require("../../models/Configuration"));
const Injectable_1 = __importDefault(require("../../decorators/Injectable"));
let ConfigureStage = class ConfigureStage extends StartupStage_1.StartupStage {
    // TODO: configure should be done in the agent class still, then do the rest on startup
    run(config) {
        return __awaiter(this, void 0, void 0, function* () {
            if (typeof config === 'string') {
                config = { apiKey: config };
            }
            Configuration_1.default.apiKey = config.apiKey;
            if (config.name) {
                Configuration_1.default.appName = config.name;
            }
            if (config.apiUrl) {
                Configuration_1.default.apiUrl = config.apiUrl;
            }
            if (config.debug) {
                Configuration_1.default.debug = config.debug;
            }
            if (config.tags) {
                Configuration_1.default.tags = config.tags;
            }
            this._complete = true;
        });
    }
};
ConfigureStage = __decorate([
    Injectable_1.default()
], ConfigureStage);
exports.default = ConfigureStage;
