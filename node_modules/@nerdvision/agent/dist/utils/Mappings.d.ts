/// <reference types="node" />
import { Debugger, InspectorNotification } from 'inspector';
export default class Mappings {
    private _seen;
    private _mapping;
    private _reverseMap;
    private _mappingParsed;
    /**
     * Set a handler to be used when a file mapping is parsed
     *
     * @param handler The handler
     */
    mappingParsed: any;
    /**
     * Process a file that was just loaded by node to see if it has source maps
     *
     * @param message The file that was loaded
     */
    process(message: InspectorNotification<Debugger.ScriptParsedEventDataType>): Promise<void>;
    has(key: string): boolean;
    get(key: string): any | undefined;
    reverseHas(key: string): boolean;
    reverseGet(key: string): any | undefined;
    seen(key: string): boolean;
}
