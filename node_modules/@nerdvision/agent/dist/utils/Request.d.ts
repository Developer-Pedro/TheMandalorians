export default class Request {
    private request;
    constructor(request: any);
    post(hostname: string, path: string, data: any, headers: any, opts?: {
        timeout?: number;
    }): Promise<any>;
}
